<Window x:Class="WhisperHeim.Views.DictationOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Whisperheim Overlay"
        Width="100" Height="40"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        ResizeMode="NoResize"
        Opacity="0">

    <Window.Resources>
        <!-- Fade in -->
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation
                Storyboard.TargetProperty="Opacity"
                To="1.0"
                Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <!-- Fade out -->
        <Storyboard x:Key="FadeOut">
            <DoubleAnimation
                Storyboard.TargetProperty="Opacity"
                To="0.0"
                Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>

    <Grid>
        <!-- Pill-shaped border -->
        <Border x:Name="PillBorder"
                CornerRadius="20"
                BorderThickness="2"
                Background="#CC2D2D2D"
                Padding="6,3">
            <Border.BorderBrush>
                <SolidColorBrush x:Name="PillBorderBrush" Color="#FF999999" />
            </Border.BorderBrush>

            <!-- Grid wrapper forces Canvas to stretch and report actual dimensions -->
            <Grid x:Name="BarsGrid">
                <Canvas x:Name="BarsCanvas"
                        ClipToBounds="True" />
            </Grid>
        </Border>
    </Grid>
</Window>
